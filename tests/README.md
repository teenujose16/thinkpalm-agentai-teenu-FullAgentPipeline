# Tests Directory

This folder is reserved for unit and integration testing of the Agentic Pipeline.

## Suggested Testing Setup
We recommend installing `vitest` or `jest` to write unit tests for the agent logic.
A sample structure would be:
- `tests/PlannerAgent.test.ts`
- `tests/AgentMemory.test.ts`

Run your suite once set up using standard npm testing pipelines.
